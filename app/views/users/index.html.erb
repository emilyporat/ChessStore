<% if logged_in? %>

  <% if current_user.role == "manager" %>

	<%= render :partial => "users/employees" %>

	<%= render :partial => "users/customers_cant_edit" %>

  <% elsif current_user.role == "admin" or current_user.role == "manager" %>

    <%= render :partial => "users/employees" %>

    <%= render :partial => "users/customers" %>

  <% end %>
  
<% end %>